// Generated by CoffeeScript 1.6.2
(function() {
  var Computer;

  Computer = (function() {
    function Computer() {}

    Computer.prototype.gameLogic = function(board) {
      this.board = board;
      if (this.canWin()) {
        return this.winningLocation();
      }
      if (this.canBlock()) {
        return this.blockLocation();
      }
      if (this.canDoubleThreat()) {
        return this.doubleThreatLocation();
      }
      if (this.canBlockDoubleThreat()) {
        return this.blockDoubleThreatLocation();
      }
      if (this.canPlayCenter()) {
        return this.playCenterLocation();
      }
      if (this.canPlayOppositeCorner()) {
        return this.playOppositeCornerLocation();
      }
      if (this.willPlayAnyCorner()) {
        return this.playAnyCornerLocation();
      }
      if (this.willPlayWall()) {
        return this.playWallLocation();
      }
    };

    Computer.prototype.canWin = function() {};

    Computer.prototype.winningLocation = function() {};

    Computer.prototype.canBlock = function() {};

    Computer.prototype.blockLocation = function() {};

    Computer.prototype.canDoubleThreat = function() {};

    Computer.prototype.doubleThreatLocation = function() {};

    Computer.prototype.canBlockDoubleThreat = function() {};

    Computer.prototype.blockDoubleThreatLocation = function() {};

    Computer.prototype.canPlayCenter = function() {};

    Computer.prototype.playCenterLocation = function() {};

    Computer.prototype.canPlayOppositeCorner = function() {};

    Computer.prototype.playOppositeCornerLocation = function() {};

    Computer.prototype.willPlayAnyCorner = function() {};

    Computer.prototype.playAnyCornerLocation = function() {};

    Computer.prototype.willPlayWall = function() {};

    Computer.prototype.playWallLocation = function() {};

    return Computer;

  })();

  window.Computer = Computer;

}).call(this);
